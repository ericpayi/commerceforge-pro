import { Area, AreaChart, Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { AlertTriangle, Boxes, DollarSign, ShoppingCart, Users } from 'lucide-react';
import { Card } from '../components/ui/Card';
import { Badge } from '../components/ui/Badge';
import { products, orders } from '../data/products';
import { formatMoney } from '../utils/money';
const revenue = [{m:'Jan',v:42000},{m:'Feb',v:58000},{m:'Mar',v:53000},{m:'Apr',v:76000},{m:'May',v:88000},{m:'Jun',v:112000}];
const categoryData = ['Electronics','Home','Outdoor','Footwear','Beauty','Fashion'].map((name) => ({ name, sales: products.filter((p) => p.category === name).reduce((s,p) => s + p.price/10, 0) }));
export function AdminDashboardPage() { const lowStock = products.filter((p) => p.stock < 15); const stats = [{icon:DollarSign,label:'Revenue',value:formatMoney(112000)},{icon:ShoppingCart,label:'Orders',value:'1,284'},{icon:Users,label:'Customers',value:'18.7k'},{icon:Boxes,label:'Inventory',value:String(products.length)}]; return <section className="section"><div className="container-page"><div className="flex items-end justify-between"><div><Badge>Admin</Badge><h1 className="mt-3 text-4xl font-black text-ink dark:text-white">Commerce operations dashboard</h1></div></div><div className="mt-8 grid gap-4 md:grid-cols-4">{stats.map((stat) => <Card key={stat.label}><stat.icon className="h-6 w-6 text-brand-600"/><p className="mt-4 text-sm text-slate-500">{stat.label}</p><strong className="text-3xl text-ink dark:text-white">{stat.value}</strong></Card>)}</div><div className="mt-8 grid gap-6 lg:grid-cols-2"><Card><h2 className="text-xl font-black text-ink dark:text-white">Revenue statistics</h2><div className="mt-6 h-80"><ResponsiveContainer><AreaChart data={revenue}><defs><linearGradient id="revenue" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="#3290ff" stopOpacity={0.6}/><stop offset="100%" stopColor="#3290ff" stopOpacity={0}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="m"/><YAxis/><Tooltip/><Area type="monotone" dataKey="v" stroke="#3290ff" fill="url(#revenue)"/></AreaChart></ResponsiveContainer></div></Card><Card><h2 className="text-xl font-black text-ink dark:text-white">Top categories</h2><div className="mt-6 h-80"><ResponsiveContainer><BarChart data={categoryData}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="sales" fill="#0f172a" radius={[8,8,0,0]}/></BarChart></ResponsiveContainer></div></Card></div><div className="mt-8 grid gap-6 lg:grid-cols-3"><Card><h2 className="text-xl font-black text-ink dark:text-white">Low stock alerts</h2><div className="mt-4 space-y-3">{lowStock.map((product) => <div key={product.id} className="flex items-center gap-3 rounded-2xl bg-amber-50 p-3 text-amber-800 dark:bg-amber-900/20 dark:text-amber-200"><AlertTriangle className="h-5 w-5"/><span className="text-sm font-bold">{product.name}: {product.stock}</span></div>)}</div></Card><Card><h2 className="text-xl font-black text-ink dark:text-white">Recent orders</h2><div className="mt-4 space-y-3">{orders.map((order) => <div key={order.id} className="rounded-2xl bg-slate-50 p-3 dark:bg-slate-800"><p className="font-bold">{order.id}</p><p className="text-sm text-slate-500">{order.status} · {formatMoney(order.total)}</p></div>)}</div></Card><Card><h2 className="text-xl font-black text-ink dark:text-white">Manage modules</h2><div className="mt-4 grid gap-3">{['Products','Categories','Users','Orders','Analytics'].map((item) => <button key={item} className="rounded-2xl border border-slate-200 p-3 text-left font-bold hover:bg-slate-50 dark:border-slate-800 dark:hover:bg-slate-800">Manage {item}</button>)}</div></Card></div></div></section>; }
